import sys
import hashlib

if len(sys.argv) != 3:
	print("Usage: python3 sql_5.py <uniqname> <password>")
	exit

uniqname = sys.argv[1]
password = sys.argv[2]

query = ""

# TODO: Set query to a value as specified in the spec. 

print(query, end="")